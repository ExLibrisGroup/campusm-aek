
var gulp = require("gulp");

gulp.task("runserver",function() {
  require("@ombiel/aek-docserver/server");
});


gulp.task("start",["runserver"],function() {
  
});

// default gulp task
gulp.task("default",['start']);

// must export this for use with aek-cli
module.exports = gulp;
