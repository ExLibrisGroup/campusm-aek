log = require("../logger")
Promise = require("rsvp").Promise
npm = require "../npm"
config = require "../config"
auth = require("../auth")

module.exports =
  name:"login"
  help:"login with App Manager credentials"
  options:[
    {
      name:"username"
      message:"username"
      required:true
      promptIfMissing:true
    }
    {
      name:"password"
      message:"password"
      required:true
      inputType:"password"
      promptIfMissing:true
    }
  ]
  callback:(options)->
    auth.authenticate(options)
    
