log = require("../logger")
auth = require("../auth")

module.exports =
  name:"auth"
  help:"Auth"
  nolist:true
  callback:(options)->
    auth.renew("apps")
      .then (data)->
        unless options.silent
          log.info(data)
      .catch (e)->
        log.error(e)

  