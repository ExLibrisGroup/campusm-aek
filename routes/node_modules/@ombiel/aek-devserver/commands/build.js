const build = require("../modules/build");


module.exports = {
  name: "build",
  help: "Build assets for production",

  options: [
    {
      name: "source",
      position: 1,
      default: "./src",
      noPrompt: true,
    },

    {
      name: "destination",
      position: 2,
      default: "./.build",
      noPrompt: true
    },

  ],

  callback: (options)=>{
    build(options);
  }
};
