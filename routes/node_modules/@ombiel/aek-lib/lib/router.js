import {Route,Router as CMRouter} from "@ombiel/cm-lib";

import {isWeb} from "./utils";

export class Router extends CMRouter {

  constructor(opts = {}) {
    if (opts.useHash == null) {
      opts.useHash = isWeb();
    }

    super(opts);
  }

}

export {Route};
