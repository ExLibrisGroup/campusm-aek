import "./base.aekcss";

import React from "react";
import PropTypes from "prop-types";

export default function Container({children, ...otherProps}) {
  return (
    <main className="container" {...otherProps}>
      <div>{children}</div>
    </main>
  );
}

Container.propTypes = {
  children: PropTypes.node,
};
