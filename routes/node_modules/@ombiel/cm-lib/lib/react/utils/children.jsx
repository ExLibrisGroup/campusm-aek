var React = require("react");

export default function getChildren(component) {
  if (!component) { return null; }
  const props = component.props || component;
  const children = props.children;
  return React.Children.toArray(children);
}
