import React from "react";
import mergeProps from "./merge-props";

export default function cloneElement(child,props) {

  props = mergeProps(child.props,props);

  return React.cloneElement(child,props);

}
