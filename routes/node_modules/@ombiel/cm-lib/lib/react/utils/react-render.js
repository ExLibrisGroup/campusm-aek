import ReactDOM from "react-dom";

export default function reactRender(content) {
  var div = document.createElement("div");
  div.id = "cm--aek--app";
  div.style.width = "100%";
  div.style.height = "100%";
  document.body.appendChild(div);
  ReactDOM.render(content,div);
}
