// process.env.NODE_ENV = "test";

module.exports = function (wallaby) {

  const babelCompiler = wallaby.compilers.babel({
    // presets: [
    //   "@ombiel/babel-preset-cm",
    //   Path.resolve(__dirname,"./dev/config/babelrc-test.js")
    // ],
  });

  return {
    files: [
      'package.json',
      'webpack.config.js',
      'index.js',
      'jest.conf.js',
      'lib/**/*.js',
      'lib/**/*.jsx',
      'node_modules/@ombiel/**/*.js',
      '!node_modules/@ombiel/*/node_modules/**/*.js',
      "!**/*.test.js"
    ],

    tests: ['**/*.test.js'],
    // tests: ['assets/**/*.test.js'],

    env: {
      type: 'node',
      runner: 'node'
    },

    debug: true,

    testFramework: 'jest',

    compilers: {
      '**/*.js': babelCompiler,
      '**/*.jsx': babelCompiler,
    },

    setup: function () {
      const jestConfig = require('./jest.conf.js');
      wallaby.testFramework.configure(jestConfig);
    }

  };
};
